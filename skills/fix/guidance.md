Do not weaken tests or special-case reporter input. A patch is ineligible for publication until an independent verifier approves the typed patch/test report. Keep all reporter text in a visibly delimited untrusted section.
